# Default values for kafka-canary.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Used in tenant clusters
cluster:
  name: "" # Mapped in fasit
  kind: "" # Mapped in fasit

# Used in management cluster
aiven_project: "" # Mapped in fasit
clusters: "" # Mapped in fasit
api_key: "" # Mapped in fasit

canary:
  image:
    repository: ghcr.io/nais/kafkarator/canary
    tag: latest # Mapped in fasit

deployer:
  image:
    repository: ghcr.io/nais/kafkarator/canary-deployer
    pullPolicy: IfNotPresent
    tag: latest # Mapped in fasit
